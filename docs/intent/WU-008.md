# WU-008 — Дымовой прогон Claude CLI

- **id:** `WU-008`
- **requirement:** `REQ-003`
- **architecture:** `ARCH-001` (нового деплой-юнита нет)
- **один носитель:** этот файл

## Критерии приёмки (дословно из REQ-003)

1. **AC-1.** В `AGENTS.md` сказано: Claude CLI — second-opinion; оракул по-прежнему `python -m pytest`; эталон и тесты не пишет.
2. **AC-2.** Есть один записанный прогон: команда, код выхода 0, текст ответа в `docs/runs/WU-008/`. `kpi/calculator.py` и `expected/kpi.csv` после прогона те же, что до него.
3. **AC-3.** Определение маршрута в репо: `.claude/agents/claude-thinking.md` (plan, opus, без записи). Не копируем кит заново.

## Вне скоупа

Codex, Gemini. Правки калькулятора. Unattended landing.
