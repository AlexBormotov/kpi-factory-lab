# REQ-003 — Second-opinion через Claude CLI

- **id:** `REQ-003` (не переиспользовать)
- **title:** Второй читатель: `claude` в plan mode, не оракул
- **owner:** владелец репозитория `test`
- **review date:** 2026-10-10
- **кто хочет исход и зачем:** человек, который учит фабрику. Нужен второй читатель (другая модель), когда основной агент уже прогнал pytest. Это не замена эталона и не Kloud Data.

## Исход

В цикле есть явный вызов Claude Code CLI. Он читает репо, пишет мнение, файлы не меняет. Расписка прогона лежит в `docs/runs/`. Человек (или фраза «отправь в Claude» / «запускай») включает вызов. Сам по зелёному CI агент Claude не вызывает.

## Критерии приёмки

1. **AC-1.** В `AGENTS.md` сказано: Claude CLI — second-opinion; оракул по-прежнему `python -m pytest`; эталон и тесты не пишет.
2. **AC-2.** Есть один записанный прогон: команда, код выхода 0, текст ответа в `docs/runs/WU-008/`. `kpi/calculator.py` и `expected/kpi.csv` после прогона те же, что до него.
3. **AC-3.** Определение маршрута в репо: `.claude/agents/claude-thinking.md` (plan, opus, без записи). Не копируем кит заново.

## Вне скоупа

- Claude как автор эталона или как G4.
- Codex / Gemini / cursor-agent в этом requirement.
- Unattended landing. G2, G5, G9.
- Менять формулу калькулятора.
