# WU-003 — Negative control оракула (G4) и закрытие G8

- **requirement:** `REQ-001` (класс: чинить калькулятор, эталон не трогать)
- **architecture:** `ARCH-001`
- **канон:** этот файл

## Критерии

1. **AC-1.** `python scripts/negative_control.py` выключает фильтр ботов в `kpi/calculator.py`, краснеют только Cost-тесты (`test_calculator_filters_and_groups`, `test_kpi_matches_expected`), generate и HTML остаются зелёными, калькулятор после прогона как был.
2. **AC-2.** Тот же скрипт в CI job `test` после pytest. Если Cost не краснеет — job красная.
3. **AC-3.** `docs/audit/gates.md`: G4 не «слабо / нет». G8 записан как **да (пусто)**: merge ≠ прод. G9 по-прежнему нет.

## Вне скоупа

CODEOWNERS на `tests/`. Deploy / CD. Unattended landing. Столп 1 (линтер).
