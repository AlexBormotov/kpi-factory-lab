# WU-002 — Ворота для класса «чинить калькулятор»

- **requirement:** `REQ-001` (класс изменений внутри уже собранного станка)
- **architecture:** `ARCH-001`
- **канон:** этот файл

## Критерии

1. **AC-1.** Файл `docs/audit/gates.md` заполнен по девяти воротам, каждая строка с вердиктом и уликой, привязка к SHA.
2. **AC-2.** В репо есть CI: GitHub Actions запускает `python -m pytest -q`.
3. **AC-3.** README говорит, что без защиты `main` на сервере CI — детектор, не ворота G7.

## Вне скоупа

Включить required status check на GitHub (нужен remote и клики в UI). Unattended landing. Столп 1 (линтер).
