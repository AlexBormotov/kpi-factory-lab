# Аудит KPI-станка

Заполнено по `ai-factory-kit/02-audit-checklist.md`. Ответы из репозитория и команд, не из намерения.

| | |
|---|---|
| Repository | `test` (папка workspace, не git) |
| Commit audited (SHA) | **нет git.** Подмена: sha256 `kpi/calculator.py` = `9ac231d819b6`. Строка **не привязана к коммиту** — дерево может уехать без следа. |
| Branch / модель | нет trunk. Нет веток. |
| Date and time | 2026-09-10, после зелёного эталона (человек заполнил `expected/kpi.csv`) |
| Auditor | агент в этой сессии |
| Suite command | `python -m pytest -q` (pytest.ini: `testpaths = tests`) |

Команда сюиты сейчас: **5 collected, 5 passed in 0.09s.** Собрано ровно 5 функций в `tests/` — молчаливых skip нет.

---

## Сводка по 11 столпам

Правила: 80% пунктов уровня внутри столпа; **столп 11 = Not pursued** (мультиагентные прогоны не ведём; файлы в `.cursor/agents/` лежат, но не вызываются).

| Столп | Уровень | Почему |
|---|---|---|
| 1 Style & Validation | **Pre-Functional** | Нет ruff/mypy/`py_compile` в гейте. Дефекты ловит только pytest. |
| 2 Build System | **L1** | `python -m pytest` работает здесь. Нет lockfile, нет CI. |
| 3 Testing | **L1** | Сюита есть и ловит пустой эталон. Нет CI, нет negative control (3.5). Одна команда есть, запуск одного теста в README не описан → L2 не набирает 80%. |
| 4 Documentation | **L2** | README, GLOSSARY, `docs/intent/*`. Нет `AGENTS.md` → не L3. |
| 5 Dev Environment | **L1** | Свежий clone не проверяли (git нет). Зависимость `pytest` не запинена. |
| 6 Debugging | **L1** | print в CLI. Нет structured log / tracker. |
| 7 Security | **L1** | Секретов в дереве не видно. Нет protected ref — git отсутствует. |
| 8 Task Discovery | **L1** | Backlog = `docs/intent/`. Нет шаблонов issue/PR. |
| 9 Product | **Pre-Functional** | Нет записей релизов. Лаборатория без выкладки. |
| 10 Agent tooling | **L1** | Cursor + skills в `.cursor/skills`. Нет `AGENTS.md`, нет allowlist команд. |
| 11 Multi-agent | **Not pursued** | Параллельных воркеров нет. Исключён из минимума. |

**Уровень репозитория: Pre-Functional.**  
Столп, который ставит минимум: **1** (и рядом 9).  
Что поднимет на ступень: git + одна команда линта/типа, которая реально падает, и `pytest` в защите ветки.

---

## Чеклист (столпы, которые решают unattended landing)

### Pillar 1

| # | Answer | Evidence |
|---|---|---|
| 1.1 | **нет** | Нет ruff, mypy, pyright. Язык Python. |
| 1.2 | нет | Нечему падать на стиле. |
| 1.3 | нет | Нет protected branch. |

### Pillar 3

| # | Answer | Evidence |
|---|---|---|
| 3.1 | **да, в `tests/`** | `--collect-only`: 5 тестов; прогон 5 passed. `ai-factory-kit/monitoring` отсечён `testpaths`. |
| 3.2 | **нет** | Нет SHA коммита, нет CI на каждый путь в trunk. |
| 3.3 | **нет** | Нет ref, который отказывается. Красный pytest — только локальный детектор. |
| 3.4 | **частично** | Дашборд читает те же `output/kpi.csv` и `pytest.json`, что пишет калькулятор. HTML сам KPI не считает. Отдельного теста «то, что в браузере = csv» нет. |
| 3.5 | **нет** | Negative control не делали: фильтр `is_bot` не выключали, чтобы покраснел только Cost. |
| 3.6 | **да (вакуум)** | Skip/`xfail` в `tests/` нет. |

### Pillar 4

| # | Answer | Evidence |
|---|---|---|
| 4.1 | **да** | README, GLOSSARY, REQ-001, ARCH-001, WU-001. |
| 4.2 | **частично** | `pytest.ini testpaths` — правило из дефекта «кит попал в сюиту». Лежит в репо. |
| 4.3 | **да для станка** | Канон: `campaign`, `day`, `cost`, `revenue`. HTML те же имена колонок. |
| 4.4 | **нет** | Comprehension debt не меряется. Ожидаемо. |

### Pillar 5

| # | Answer | Evidence |
|---|---|---|
| 5.1 | **не доказано** | Нет git → нет fresh clone. Прогон был в уже живой машине с pytest. |
| 5.2 | **нет** | Нет `requirements.txt` / lock. |
| 5.3 | **частично** | README достаточен человеку, знающему Python. Версия pytest не зафиксирована. |

### Pillar 7

| # | Answer | Evidence |
|---|---|---|
| 7.1 | **конвенция, не правило** | Edit surface описан в README. Агент всё ещё *может* править эталон: нет хука/CI, который это запретит. |
| 7.2 | **да для этого продукта** | Нет auth/денег/миграций. Класс «калькулятор» вне G3-исключений. |
| 7.3 | **нет** | Kill switch не искали и не дёргали. |

### Pillar 11

| # | Answer | Evidence |
|---|---|---|
| 11.1 | **нет / не применяется** | Один working tree, без worktree. Порт 8765 был разовым для проверки HTML. |
| 11.2 | **да, для KPI** | Эталон `expected/kpi.csv` писал человек, не модель. |

---

## Исход 2 (заготовка шага 5): класс изменений

| | |
|---|---|
| Class of change | Починить `kpi/calculator.py`, не трогая фикстуры и `expected/kpi.csv` |
| May it update trunk unattended | **no** |
| Size ceiling | не из p75: **нет git history** |
| Paths excluded | `expected/`, `fixtures/`, `tests/`, `generate.py` |
| Review date | 2026-10-09 |

### Девять ворот (сейчас все, кроме G1/G3, не проходят)

| Gate | Verdict | Evidence |
|---|---|---|
| G1 Scope | **pass** | Один файл, условие: pytest зелёный против эталона. |
| G2 Maturity | **fail** | Репо Pre-Functional, не L3. |
| G3 Risk | **pass** | Не auth, не деньги, не схема, не публичный API банка. |
| G4 Oracle | **weak / fail** | Эталон человеческий и дешёвый. Нет negative control. Нет запрета агенту править тесты — только текст в README. |
| G5 Harness | **fail** | Нет изолированного candidate SHA. |
| G6 Evidence | **fail** | `output/pytest.json` есть, но не привязан к git SHA. |
| G7 Independent landing | **fail** | Нет серверной проверки SHA. Git нет. |
| G8 Exposure | **pass (вакуум)** | Деплоя в прод нет; сажать trunk ≠ выкатывать банк. |
| G9 Recovery | **fail** | Нет last green SHA, нечего revert. |

---

## Closing note

1. **Одно изменение с наибольшим эффектом:** сделать git и повесить на push/PR ту же команду `python -m pytest -q`. Без этого нет G2/G6/G7, столпы 3 и 7 остаются детектором.
2. **Чему верят без доказательства:** что агент не тронет эталон; что зелёный pytest = правильный HTML; что фильтр ботов вообще проверяется (нет negative control).
3. **Что не аудировали:** branch protection (нечего читать); fresh container; столп 9 как продукт; `REQ-002` (12 файлов кита).

Дальше по плану — **шаг 5 уже начат таблицей ворот:** вердикт «без человека в trunk нельзя». Можно остановиться здесь или чинить самое узкое (git + pytest как гейт), не притворяясь L3.
