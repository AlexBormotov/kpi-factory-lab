# WU-001 run

Старт реализации. Intent: `docs/intent/WU-001.md`.

## План

1. Тесты на калькулятор, генератор, пустой эталон, витрину — сначала красные.
2. `generate.py`, `kpi/calculator.py`, `kpi/evidence.py`, `kpi/report.py`.
3. Запустить генератор один раз, закоммитить результат в working tree.
4. Зафиксировать команды и коды выхода в `evidence.md`.

Rung 3 (simplicity-ladder): в репо нет калькулятора; stdlib + pytest.
