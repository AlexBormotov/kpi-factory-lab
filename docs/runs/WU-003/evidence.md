# Evidence WU-003

Другой человек может повторить команды. Чат не нужен.

## Прогоны (локально, до PR)

| команда | код | заметка |
|---|---|---|
| `python scripts/negative_control.py` | 0 | Cost-тесты красные на сломанном фильтре; generate/HTML зелёные; файл восстановлен |
| `python -m pytest -q` | 0 | 5 passed после восстановления |

CI повторит оба шага в job `test` на GitHub.
