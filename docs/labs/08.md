# Глава 08 — ledger одного дня

День: 2026-09-10, лабораторный контур REQ-002 (не прод).

**Метрика:** `python -m pytest -q` → 5 passed, код выхода 0. Лучше = зелёный и без роста числа тестов за счёт ослабления эталона. Эталон не трогаем.

**Бюджет сравнения:** одна единица → один PR → один прогон CI job `test`. Другой бюджет = другой ledger.

**Edit surface этого дня:** intent и `docs/labs/`, не `kpi/calculator.py`.

| id | SHA | метрика | keep / discard |
|---|---|---|---|
| B0 | `973624b` | 5 passed (до REQ-002) | **baseline** |
| K1 | `597fe63` (PR #3) | 5 passed | **keep** |
| K2 | `ea9abc1` (PR #4) | 5 passed | **keep** |
| K3 | `d1b9bc6` (PR #5) | 5 passed | **keep** |
| D | — | — | **discard: 0** |

Discard пустой не потому что «всё идеально». В этот день не было кандидата, который сломал оракул и был отвергнут. Сломанный фильтр ботов живёт только внутри `scripts/negative_control.py` и всегда откатывается.

Это rehearsal главы 08, не право на unattended landing.
