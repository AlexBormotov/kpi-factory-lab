# Глава 12 — N/A

**Статус: N/A.** Один loop, памяти сессии нет.

`12-harness-runtime.md` про длинный или общий агент: гибридная память, смена машины посреди задачи, бюджеты persistence. Триггер не держится.

У нас: один каталог, один агент в чате, нет subagent с checkpoint, нет «вспомнить наблюдение после вытеснения промпта». Run record на диске (`docs/runs/`) — это улика G6, не runtime-память главы 12.

Когда появится агент, который переживает смену среды или сессии — открыть отдельный REQ, не дописывать сюда.
